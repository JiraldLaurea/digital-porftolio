export type Tech = {
    imgSrc: string;
    techName: string;
};

export type Task = {
    description: string;
};

export type Skill = {
    imgSrc: string;
    techName: string;
    level: number;
    mounted?: boolean;
};
